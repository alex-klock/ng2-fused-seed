import { Sparky } from 'fuse-box';
import { NgBuildContext } from '../ng-build-context';

const config = NgBuildContext.config;
const paths = NgBuildContext.paths;

Sparky.task('test:init', () => {
});